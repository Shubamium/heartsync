import { GlobalConfig } from "payload";

const PrivacyPolicy: GlobalConfig = {
  slug: "privacy-policy",
  fields: [
    {
      name: "text",
      type: "richText",
    },
  ],
};

export default PrivacyPolicy;
