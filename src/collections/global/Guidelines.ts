import { GlobalConfig } from "payload";

const Guidelines: GlobalConfig = {
  slug: "guidelines",
  fields: [
    {
      name: "guideline",
      type: "richText",
    },
  ],
};

export default Guidelines;
